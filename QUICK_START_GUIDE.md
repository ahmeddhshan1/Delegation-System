# 🚀 دليل التشغيل السريع - Delegation Management System

## 📌 نظرة عامة
نظام إدارة الوفود العسكرية والمدنية - نظام شامل لإدارة الفعاليات والوفود.

---

## 🎯 خطوات التشغيل على جهاز جديد

### المتطلبات الأساسية (يجب تثبيتها أولاً)

1. **Python 3.8+** 
   - تحميل: https://www.python.org/downloads/
   - ⚠️ مهم: اختر "Add Python to PATH" أثناء التثبيت

2. **Node.js 18+**
   - تحميل: https://nodejs.org/
   - سيتم تثبيت npm تلقائياً

3. **PostgreSQL**
   - تحميل: https://www.postgresql.org/download/
   - ⚠️ احفظ كلمة المرور التي تدخلها أثناء التثبيت

---

## 📋 الخطوات التفصيلية

### ✅ الخطوة 1: إعداد قاعدة البيانات PostgreSQL

بعد تثبيت PostgreSQL:

#### افتح PostgreSQL (أحد الطرق التالية):
- **pgAdmin** (واجهة رسومية)
- **SQL Shell (psql)** (سطر الأوامر)
- **Command Prompt** وكتابة: `psql -U postgres`

#### نفذ هذه الأوامر SQL:

```sql
-- إنشاء قاعدة البيانات
CREATE DATABASE delegation_system;

-- إنشاء المستخدم (إذا لم يكن موجوداً)
-- CREATE USER postgres WITH PASSWORD '722003';

-- منح الصلاحيات
GRANT ALL PRIVILEGES ON DATABASE delegation_system TO postgres;

-- إعدادات إضافية (اختيارية لكن موصى بها)
ALTER ROLE postgres SET client_encoding TO 'utf8';
ALTER ROLE postgres SET default_transaction_isolation TO 'read committed';
ALTER ROLE postgres SET timezone TO 'Africa/Cairo';
```

**ملاحظة:** إذا كانت كلمة مرور PostgreSQL عندك مختلفة عن `722003`، احفظها لأنك ستحتاجها لاحقاً.

---

### ✅ الخطوة 2: نسخ المشروع

انسخ مجلد المشروع بالكامل إلى الجهاز الجديد:
```
📁 Delegation-System/
```

---

### ✅ الخطوة 3: تثبيت المتطلبات

افتح Command Prompt أو PowerShell في مجلد المشروع وشغّل:

```bash
install_all.bat
```

**هذا الملف سيقوم بـ:**
- ✅ التحقق من وجود Python و Node.js
- ✅ إنشاء البيئة الافتراضية Python (venv)
- ✅ تثبيت جميع حزم Python (Django، وغيرها)
- ✅ تثبيت جميع حزم Node.js (React، وغيرها)
- ✅ إنشاء ملف `.env` تلقائياً
- ✅ تشغيل migrations لقاعدة البيانات
- ✅ إنشاء ملفات static

**⏱️ المدة المتوقعة:** 5-10 دقائق

---

### ✅ الخطوة 4: تعديل إعدادات قاعدة البيانات (إذا لزم الأمر)

إذا كانت كلمة مرور PostgreSQL عندك **مختلفة** عن `722003`:

1. افتح الملف: `Delegation-Backend\.env`
2. ابحث عن السطر: `DB_PASSWORD=722003`
3. غيره إلى كلمة المرور الصحيحة: `DB_PASSWORD=your-password`
4. احفظ الملف

---

### ✅ الخطوة 5: تشغيل النظام

شغّل الملف:

```bash
run_all.bat
```

**سيتم فتح نافذتين:**
- 🔵 **Backend Server** - Django (Port 8000)
- 🟢 **Frontend Server** - React + Vite (Port 5173)

⚠️ **لا تغلق النافذتين!** السيرفرات تعمل فيهما.

---

### ✅ الخطوة 6: افتح المتصفح

النظام جاهز! افتح المتصفح على:

- **الواجهة الأمامية (Frontend):** http://localhost:5173
- **Backend API:** http://localhost:8000/api/
- **Django Admin Panel:** http://localhost:8000/admin/

---

## 🛑 إيقاف النظام

لإيقاف السيرفرات:
- **اغلق النافذتين** التي فتحها `run_all.bat`
- أو اضغط `Ctrl + C` في كل نافذة

---

## ❓ حل المشاكل الشائعة

### مشكلة: "Django not found"

**السبب:** البيئة الافتراضية غير مفعلة أو لم يتم التثبيت بنجاح.

**الحل:**
```bash
cd Delegation-Backend
venv\Scripts\activate.bat
pip install -r requirements.txt
```

---

### مشكلة: "Can't connect to database"

**السبب:** قاعدة البيانات غير موجودة أو PostgreSQL لا يعمل.

**الحل:**
1. تأكد من تشغيل PostgreSQL:
   - Windows: ابحث عن "Services" → ابحث عن "PostgreSQL" → Start
2. تأكد من إنشاء قاعدة البيانات `delegation_system`
3. تحقق من كلمة المرور في `Delegation-Backend\.env`

---

### مشكلة: "Port already in use"

**السبب:** Port 8000 أو 5173 مستخدم من تطبيق آخر.

**الحل:**
- أغلق التطبيق الآخر
- أو غيّر Port في إعدادات المشروع

---

### مشكلة: "npm install failed"

**السبب:** مشكلة في الاتصال بالإنترنت أو npm cache.

**الحل:**
```bash
cd Delegation-Front
npm cache clean --force
npm install
```

---

## 📚 ملفات مهمة

| الملف | الوصف |
|-------|-------|
| `install_all.bat` | تثبيت جميع المتطلبات تلقائياً |
| `run_all.bat` | تشغيل Backend و Frontend معاً |
| `Delegation-Backend\.env` | إعدادات قاعدة البيانات والنظام |
| `requirements.txt` | حزم Python المطلوبة |
| `requirements_all.txt` | قائمة شاملة بجميع الحزم |
| `package.json` | حزم Node.js المطلوبة |
| `INSTALLATION_GUIDE.md` | دليل التثبيت التفصيلي |

---

## 🔧 أوامر مفيدة

### Backend:
```bash
cd Delegation-Backend
venv\Scripts\activate.bat
python manage.py runserver          # تشغيل السيرفر
python manage.py makemigrations     # إنشاء migrations
python manage.py migrate            # تطبيق migrations
python manage.py createsuperuser    # إنشاء مستخدم Admin
python manage.py check              # فحص النظام
```

### Frontend:
```bash
cd Delegation-Front
npm run dev        # تشغيل سيرفر التطوير
npm run build      # بناء للإنتاج
npm run preview    # معاينة البناء
```

---

## 📊 التقنيات المستخدمة

### Backend:
- Django 5.2.7
- Django REST Framework 3.15.2
- PostgreSQL
- Django Channels (WebSocket)
- Python 3.8+

### Frontend:
- React 19.1.1
- Redux Toolkit
- Vite 7.1.2
- Tailwind CSS 4.1.13
- Shadcn UI Components

---

## 📞 معلومات إضافية

### المنافذ (Ports):
- **Backend:** 8000
- **Frontend:** 5173
- **PostgreSQL:** 5432 (افتراضي)

### المستخدم الافتراضي:
بعد التثبيت، قد تحتاج لإنشاء مستخدم Admin:
```bash
cd Delegation-Backend
venv\Scripts\activate.bat
python manage.py createsuperuser
```

---

## ✅ قائمة التحقق السريعة

قبل التشغيل، تأكد من:

- [ ] Python 3.8+ مثبت
- [ ] Node.js 18+ مثبت
- [ ] PostgreSQL مثبت ويعمل
- [ ] قاعدة البيانات `delegation_system` موجودة
- [ ] تم تشغيل `install_all.bat` بنجاح
- [ ] ملف `.env` موجود في `Delegation-Backend\`
- [ ] كلمة مرور قاعدة البيانات صحيحة في `.env`

---

## 🎯 ملخص الخطوات (TL;DR)

```bash
# 1. ثبت: Python, Node.js, PostgreSQL
# 2. أنشئ قاعدة البيانات في PostgreSQL
CREATE DATABASE delegation_system;

# 3. ثبت المتطلبات
install_all.bat

# 4. عدّل كلمة المرور في .env (إذا لزم)
# Delegation-Backend\.env

# 5. شغّل النظام
run_all.bat

# 6. افتح المتصفح
http://localhost:5173
```

---

## 📄 الترخيص
هذا المشروع ملكية خاصة - Proprietary Software

---

**تم إنشاؤه بواسطة:** Ahmed  
**آخر تحديث:** نوفمبر 2025  
**الإصدار:** 1.0.0

---

**🎉 بالتوفيق في استخدام النظام!**
