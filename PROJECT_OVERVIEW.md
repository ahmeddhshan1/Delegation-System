# 🏛️ نظام إدارة الوفود - Project Overview

## 📋 نظرة عامة على المشروع

**نظام إدارة الوفود** هو نظام شامل لإدارة الوفود العسكرية والمدنية للفعاليات الكبرى مثل:
- **EDEX** (معرض الدفاع)
- **Equestrian** (الفروسية)
- وغيرها من الفعاليات الرسمية

---

## 🎯 الهدف من النظام

إدارة وتتبع الوفود القادمة من مختلف الدول، بدءاً من:
1. **الوصول** (Flight details, Airport info)
2. **الإقامة** (Delegation members, Leader info)
3. **المغادرة** (Check-out sessions)

---

## 🏗️ المعمارية (Architecture)

### Backend (Django + PostgreSQL)
```
Backend Server (Django 5.2.7)
├── REST API (Django REST Framework)
├── WebSocket (Django Channels + Daphne)
├── Database (PostgreSQL)
└── Authentication & Authorization
```

### Frontend (React + Vite)
```
Frontend (React 19)
├── UI Components (Shadcn UI + Radix UI)
├── State Management (Redux Toolkit)
├── Routing (React Router 7)
├── Styling (Tailwind CSS 4)
└── Real-time Updates (WebSocket)
```

---

## 📊 قاعدة البيانات - Database Schema

### الجداول الرئيسية (Main Tables):

#### 1️⃣ **Users** (المستخدمون)
نظام صلاحيات متدرج من 3 مستويات:
- **SUPER_ADMIN**: صلاحيات كاملة + Django Admin Dashboard
- **ADMIN**: صلاحيات كاملة في Frontend فقط
- **USER**: صلاحيات محدودة (عرض وإضافة)

```
الحقول:
- username (اسم المستخدم)
- full_name (الاسم الكامل)
- role (الدور)
- is_active (نشط/غير نشط)
- is_staff (وصول Django Admin)
```

#### 2️⃣ **MainEvent** (الأحداث الرئيسية)
الفعاليات الكبرى مثل EDEX، Equestrian
```
الحقول:
- event_name (اسم الحدث)
- event_link (رابط الحدث)
- event_icon (أيقونة الحدث)
```

#### 3️⃣ **SubEvent** (الأحداث الفرعية)
أحداث فرعية تحت كل حدث رئيسي
```
الحقول:
- main_event_id (الحدث الرئيسي)
- event_name (اسم الحدث الفرعي)
```

#### 4️⃣ **Delegation** (الوفود)
الوفود القادمة للفعاليات
```
الحقول الرئيسية:
- sub_event_id (الحدث الفرعي)
- nationality_id (الجنسية)
- delegation_leader_name (اسم رئيس الوفد)
- type (نوع الوفد: MILITARY/CIVILIAN)
- status (حالة الوفد: NOT_DEPARTED/PARTIALLY_DEPARTED/FULLY_DEPARTED)
- member_count (العدد المحدد للأعضاء)
- current_members (العدد الحالي)

بيانات الرحلة:
- airport_id (المطار)
- airline_id (شركة الطيران)
- flight_number (رقم الرحلة)
- arrive_date (تاريخ الوصول)
- arrive_time (ساعة الوصول)
- city_id (المدينة المتجهة إليها)

بيانات إضافية:
- receiver_name (اسم المستقبل)
- going_to (الوجهة)
- goods (الشحنات)
```

#### 5️⃣ **Member** (أعضاء الوفود)
أعضاء كل وفد
```
الحقول:
- delegation_id (الوفد)
- rank (الرتبة العسكرية - للعسكريين)
- name (اسم العضو)
- job_title (الوظيفة/الدور الحالي)
- equivalent_job_id (الوظيفة المعادلة)
- status (حالة العضو: NOT_DEPARTED/DEPARTED)
- departure_date (تاريخ المغادرة الفعلي)
```

#### 6️⃣ **CheckOut** (جلسات المغادرة)
تسجيل مغادرة الوفود
```
الحقول:
- delegation_id (الوفد)
- checkout_date (تاريخ المغادرة)
- checkout_time (ساعة المغادرة)
- flight_number (رقم رحلة المغادرة)
- airport_id (المطار)
- airline_id (شركة الطيران)
- city_id (المدينة المتجهة إليها)
- depositor_name (اسم المودع)
- goods (الشحنات)
- members (قائمة الأعضاء JSON)
- notes (ملاحظات)
```

### جداول البيانات المساعدة (Lookup Tables):

- **Nationality** (الجنسيات)
- **Cities** (المدن)
- **AirLine** (شركات الطيران)
- **AirPort** (المطارات)
- **EquivalentJob** (الوظائف المعادلة)

---

## 🔄 دورة العمل (Workflow)

### 1️⃣ إنشاء الفعاليات
```
Admin → إنشاء حدث رئيسي (EDEX)
     → إنشاء أحداث فرعية (EDEX 2024, EDEX 2025)
```

### 2️⃣ تسجيل الوفود
```
User → اختيار الحدث الفرعي
    → إدخال بيانات الوفد:
       - رئيس الوفد
       - الجنسية
       - نوع الوفد (عسكري/مدني)
       - بيانات الرحلة (مطار، شركة طيران، رقم رحلة، موعد)
       - عدد الأعضاء المتوقع
```

### 3️⃣ إضافة الأعضاء
```
User → اختيار الوفد
    → إضافة أعضاء:
       - للعسكريين: الرتبة + الاسم + الوظيفة
       - للمدنيين: الاسم + الوظيفة
       - الوظيفة المعادلة
```

### 4️⃣ تتبع الحالة
```
النظام يحدث تلقائياً:
- current_members (عند إضافة/حذف عضو)
- status (حالة الوفد بناءً على مغادرة الأعضاء)
```

### 5️⃣ تسجيل المغادرة (Check-out)
```
User → إنشاء جلسة مغادرة
    → اختيار الأعضاء المغادرين
    → إدخال بيانات الرحلة
    → حفظ الجلسة
```

---

## 🌐 API Endpoints

### Authentication
```
POST   /api/auth/login/          # تسجيل الدخول
POST   /api/auth/logout/         # تسجيل الخروج
GET    /api/auth/check/          # التحقق من الجلسة
```

### Users Management
```
GET    /api/users/               # قائمة المستخدمين
POST   /api/users/               # إنشاء مستخدم جديد
GET    /api/users/{id}/          # تفاصيل مستخدم
PUT    /api/users/{id}/          # تعديل مستخدم
DELETE /api/users/{id}/          # حذف مستخدم
```

### Events
```
GET    /api/main-events/         # الأحداث الرئيسية
POST   /api/main-events/         # إنشاء حدث رئيسي
GET    /api/sub-events/          # الأحداث الفرعية
POST   /api/sub-events/          # إنشاء حدث فرعي
```

### Delegations
```
GET    /api/delegations/         # قائمة الوفود
POST   /api/delegations/         # إنشاء وفد جديد
GET    /api/delegations/{id}/    # تفاصيل وفد
PUT    /api/delegations/{id}/    # تعديل وفد
DELETE /api/delegations/{id}/    # حذف وفد
```

### Members
```
GET    /api/members/             # قائمة الأعضاء
POST   /api/members/             # إضافة عضو
GET    /api/members/{id}/        # تفاصيل عضو
PUT    /api/members/{id}/        # تعديل عضو
DELETE /api/members/{id}/        # حذف عضو
```

### Check-outs
```
GET    /api/check-outs/          # جلسات المغادرة
POST   /api/check-outs/          # إنشاء جلسة مغادرة
GET    /api/check-outs/{id}/     # تفاصيل جلسة
PUT    /api/check-outs/{id}/     # تعديل جلسة
DELETE /api/check-outs/{id}/     # حذف جلسة
```

### Statistics
```
GET    /api/stats/               # إحصائيات النظام
```

### Lookup Data
```
GET    /api/nationalities/       # الجنسيات
GET    /api/cities/              # المدن
GET    /api/airlines/            # شركات الطيران
GET    /api/airports/            # المطارات
GET    /api/equivalent-jobs/     # الوظائف المعادلة
```

---

## 🎨 Frontend Structure

### الصفحات الرئيسية (Pages):

1. **Login** (`/login`)
   - تسجيل الدخول للنظام

2. **Home** (`/`)
   - Dashboard رئيسية
   - إحصائيات عامة
   - آخر التحديثات

3. **Events** (`/events`)
   - عرض الأحداث الرئيسية
   - إنشاء/تعديل/حذف الأحداث

4. **Sub-Events** (`/sub-events`)
   - عرض الأحداث الفرعية
   - إدارة الأحداث الفرعية

5. **Delegations** (تحت كل حدث فرعي)
   - قائمة الوفود
   - تفاصيل كل وفد
   - إضافة/تعديل/حذف وفود

6. **Members** (`/members`)
   - قائمة جميع الأعضاء
   - البحث والفلترة
   - إدارة الأعضاء

7. **Check-outs** (داخل صفحة الوفد)
   - جلسات المغادرة
   - تسجيل مغادرة جديدة

### المكونات الرئيسية (Components):

```
components/
├── Auth/
│   ├── Login.jsx               # صفحة تسجيل الدخول
│   └── ProtectedRoute.jsx      # حماية الصفحات
│
├── Events/
│   ├── MainEventCard.jsx       # كارد الحدث الرئيسي
│   ├── SubEventCard.jsx        # كارد الحدث الفرعي
│   └── EventForm.jsx           # فورم إنشاء/تعديل حدث
│
├── Delegations/
│   ├── DelegationCard.jsx      # كارد الوفد
│   ├── DelegationForm.jsx      # فورم إضافة وفد
│   ├── DelegationDetails.jsx   # تفاصيل الوفد
│   └── DelegationList.jsx      # قائمة الوفود
│
├── Members/
│   ├── MemberCard.jsx          # كارد العضو
│   ├── MemberForm.jsx          # فورم إضافة عضو
│   └── MembersList.jsx         # قائمة الأعضاء
│
├── PDF Templates/
│   ├── DelegationPDF.jsx       # تقرير PDF للوفد
│   └── AllMembersPDF.jsx       # تقرير PDF لجميع الأعضاء
│
├── Header.jsx                  # Header العلوي
├── SideBar.jsx                 # القائمة الجانبية
├── Stats.jsx                   # الإحصائيات
└── WebSocketStatus.jsx         # حالة WebSocket
```

---

## 🔐 نظام الصلاحيات

### SUPER_ADMIN (مدير النظام)
✅ جميع الصلاحيات:
- إنشاء/تعديل/حذف المستخدمين
- الوصول إلى Django Admin Panel
- إدارة جميع الأحداث والوفود
- إدارة البيانات المساعدة (Lookup tables)
- عرض جميع الإحصائيات

### ADMIN (مدير)
✅ صلاحيات كاملة في Frontend:
- إنشاء/تعديل/حذف الأحداث
- إدارة الوفود والأعضاء
- تسجيل المغادرة
- عرض التقارير والإحصائيات
❌ لا يمكنه:
- الوصول إلى Django Admin
- إدارة المستخدمين

### USER (مستخدم)
✅ صلاحيات محدودة:
- عرض الأحداث والوفود
- إضافة وفود جديدة
- إضافة أعضاء
❌ لا يمكنه:
- حذف أو تعديل بيانات موجودة
- تسجيل المغادرة
- إدارة الأحداث

---

## 📊 التقارير والتصدير

### تصدير PDF:
- تقرير تفصيلي لكل وفد
- قائمة جميع أعضاء الوفد
- معلومات الرحلة والمغادرة

### تصدير Excel:
- قائمة الوفود بجميع البيانات
- قائمة الأعضاء
- جلسات المغادرة

---

## 🔄 التحديثات الفورية (Real-time)

### WebSocket Integration:
```
Client → WebSocket → Server
   ↓                    ↓
   ← Push updates ←
```

**التحديثات الفورية تشمل:**
- إضافة/تعديل/حذف وفد
- إضافة/تعديل/حذف عضو
- تحديث حالة الوفد
- تسجيل مغادرة

**WebSocket URL:**
```
ws://localhost:8000/ws/updates/
```

---

## 🗂️ هيكل المشروع

```
Delegation-System/
│
├── Delegation-Backend/          # Backend Django
│   ├── api/                     # API App
│   │   ├── models.py           # Database Models
│   │   ├── views.py            # API Views
│   │   ├── serializers.py      # DRF Serializers
│   │   └── urls.py             # API URLs
│   │
│   ├── accounts/                # Authentication App
│   │   ├── models.py           # User Model
│   │   └── views.py            # Auth Views
│   │
│   ├── delegation_system/       # Project Settings
│   │   ├── settings.py         # Django Settings
│   │   ├── urls.py             # Main URLs
│   │   └── asgi.py             # ASGI Config (WebSocket)
│   │
│   ├── manage.py                # Django Management
│   ├── requirements.txt         # Python Dependencies
│   └── .env                     # Environment Variables
│
├── Delegation-Front/            # Frontend React
│   ├── src/
│   │   ├── components/         # React Components
│   │   ├── pages/              # Pages
│   │   ├── store/              # Redux Store
│   │   ├── contexts/           # React Contexts
│   │   ├── hooks/              # Custom Hooks
│   │   ├── utils/              # Utilities
│   │   └── main.jsx            # Entry Point
│   │
│   ├── package.json            # Node Dependencies
│   └── .env                    # Environment Variables
│
├── install_all.bat             # تثبيت كل المتطلبات
├── run_all.bat                 # تشغيل النظام
├── requirements_all.txt        # جميع متطلبات Python
├── database_schema.sql         # قاعدة البيانات SQL
│
├── README.md                   # وصف المشروع
├── INSTALLATION_GUIDE.md       # دليل التثبيت التفصيلي
├── QUICK_START_GUIDE.md        # دليل التشغيل السريع
└── PROJECT_OVERVIEW.md         # هذا الملف
```

---

## 🌐 الوصول من الشبكة (Network Access)

### للتطوير المحلي:
```env
VITE_API_URL=http://localhost:8000/api
VITE_WS_URL=ws://localhost:8000/ws/updates/
```

### للوصول من أجهزة أخرى على نفس الشبكة:
```env
VITE_API_URL=http://1.16.34.85:8000/api
VITE_WS_URL=ws://1.16.34.85:8000/ws/updates/
```

**ملاحظة:** يجب تشغيل Backend بـ:
```bash
python manage.py runserver 0.0.0.0:8000
```

وتأكد من إضافة IP في Backend `.env`:
```env
ALLOWED_HOSTS=localhost,127.0.0.1,1.16.34.85,*
```

---

## 🔧 المشاكل الشائعة وحلولها

### 1. Login لا يعمل ❌

**السبب:** Frontend يتصل بعنوان خاطئ

**الحل:**
- تحقق من `.env` في `Delegation-Front/`
- تأكد من أن `VITE_API_URL` يشير إلى Backend الصحيح
- إذا كنت تستخدم IP الشبكة، تأكد من أن Backend يعمل على `0.0.0.0:8000`

### 2. WebSocket Errors ⚠️

**السبب:** عدم الاتصال بـ WebSocket

**الحل:**
- تحقق من `VITE_WS_URL` في `.env`
- تأكد من تشغيل Daphne (مدمج في `runserver`)
- تحقق من `ALLOWED_HOSTS` في Backend

### 3. Database Connection Failed 🔴

**السبب:** PostgreSQL لا يعمل أو كلمة المرور خاطئة

**الحل:**
```bash
# 1. تحقق من PostgreSQL
# Windows: Services → PostgreSQL → Start

# 2. تحقق من .env في Backend
DB_NAME=delegation_system
DB_USER=postgres
DB_PASSWORD=722003  # تأكد من صحة كلمة المرور
DB_HOST=localhost
DB_PORT=5432
```

### 4. Port Already in Use 🚫

**السبب:** Port مستخدم من تطبيق آخر

**الحل:**
```bash
# Windows: إيقاف البروسس على Port 8000
netstat -ano | findstr :8000
taskkill /PID <process_id> /F
```

---

## 📈 الإحصائيات المتاحة

1. **عدد الوفود الإجمالي**
2. **عدد الأعضاء الإجمالي**
3. **عدد الأحداث النشطة**
4. **توزيع الوفود حسب الجنسية**
5. **توزيع الوفود حسب النوع (عسكري/مدني)**
6. **حالات الوفود (لم يغادر/غادر جزئياً/غادر بالكامل)**
7. **جلسات المغادرة**

---

## 🎯 حالات الاستخدام (Use Cases)

### Scenario 1: تسجيل وفد جديد
```
1. Admin يسجل دخول النظام
2. يختار الحدث الفرعي (مثل EDEX 2024)
3. ينقر "إضافة وفد جديد"
4. يدخل البيانات:
   - رئيس الوفد: "اللواء محمد أحمد"
   - الجنسية: "السعودية"
   - النوع: "عسكري"
   - معلومات الرحلة
   - عدد الأعضاء المتوقع: 10
5. يحفظ البيانات
6. النظام يرسل تحديث WebSocket لجميع المستخدمين
```

### Scenario 2: إضافة أعضاء الوفد
```
1. User يفتح تفاصيل الوفد
2. ينقر "إضافة عضو"
3. يدخل:
   - الرتبة: "عقيد"
   - الاسم: "أحمد محمود"
   - الوظيفة: "مستشار عسكري"
   - الوظيفة المعادلة: "مدير"
4. يحفظ
5. النظام يحدث current_members تلقائياً
```

### Scenario 3: تسجيل مغادرة
```
1. Admin يفتح صفحة الوفد
2. ينقر "تسجيل مغادرة"
3. يختار الأعضاء المغادرين
4. يدخل معلومات الرحلة
5. يحفظ الجلسة
6. النظام يحدث حالة الأعضاء إلى "DEPARTED"
7. يحدث حالة الوفد (جزئي/كامل) تلقائياً
```

---

## 📝 ملاحظات مهمة للتطوير

### 1. Database Migrations
```bash
cd Delegation-Backend
python manage.py makemigrations
python manage.py migrate
```

### 2. إنشاء مستخدم Admin
```bash
python manage.py createsuperuser
```

### 3. تجميع Static Files
```bash
python manage.py collectstatic
```

### 4. Testing API
استخدم Postman collection في:
```
Delegation-Backend/postman_collection.json
```

---

## 🚀 خريطة الطريق (Roadmap)

### الميزات المستقبلية المقترحة:

1. **إشعارات Push Notifications**
   - تنبيهات عند وصول وفد جديد
   - تذكير بمواعيد المغادرة

2. **تقارير متقدمة**
   - تقارير شهرية/سنوية
   - رسوم بيانية تفاعلية

3. **نظام الأرشفة**
   - أرشفة الأحداث القديمة
   - استرجاع البيانات التاريخية

4. **تكامل مع أنظمة خارجية**
   - API للأنظمة الأخرى
   - تزامن البيانات

5. **تطبيق موبايل**
   - iOS & Android App
   - متابعة في الوقت الفعلي

---

## 🤝 المساهمة والدعم

### للمساهمة في المشروع:
1. Fork the repository
2. Create a feature branch
3. Make your changes
4. Submit a pull request

### للإبلاغ عن مشاكل:
- افتح Issue في GitHub
- أو اتصل بفريق التطوير

---

## 📄 الترخيص

هذا المشروع **ملكية خاصة** (Proprietary Software)

جميع الحقوق محفوظة © 2024-2025

---

## 👥 معلومات التواصل

**المطور:** Ahmed  
**النسخة:** 1.0.0  
**آخر تحديث:** نوفمبر 2025

---

## 🎉 شكر خاص

شكراً لاستخدام نظام إدارة الوفود!

نتمنى أن يكون هذا الدليل مفيداً في فهم النظام وكيفية استخدامه.

**للدعم الفني أو الاستفسارات، يرجى التواصل مع فريق التطوير.**

---

**تم إنشاء هذا المستند بواسطة:** AI Assistant  
**التاريخ:** نوفمبر 2025
